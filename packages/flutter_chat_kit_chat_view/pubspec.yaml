name: flutter_chat_kit_chat_view
description: "A customizable chat view widget for Flutter applications."
version: 0.0.1

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: